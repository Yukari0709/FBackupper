#include "BackupHelper.h"

void BackupHelper::doBackup(std::vector<File> file_names, std::string input_path){
    // TODO：
}

BackupHelper::BackupHelper(const Paras &p): _compress(p.compress), _encrypt(p.encrypt){
    
}